# Development & test tooling (never installed in production images).
-r requirements.txt

pytest>=8.3,<9.0
pytest-cov>=6.0,<8.0
httpx>=0.28,<1.0        # required by fastapi.testclient
ruff>=0.8,<1.0          # linter + formatter
